#!/bin/bash
# Husky prepare-commit-msg hook
# Validates commit message format (Conventional Commits)
# Optional: helps format commit message interactively

# This hook is run when a commit message is being prepared
# It can enforce or suggest commit message format

exit 0
